<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BackgroundCards" parent="AppTheme">
        <item name="android:background">@color/colorSecondText</item>
    </style>

    <style name="ItemBackgroundSelectable" parent="AppTheme">
        <item name="android:background">?attr/selectableItemBackground</item>
    </style>

    <style name="WidgetStyle" parent="">
        <item name="android:background">@color/colorBackgroundWidget</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.ToolbarDark" parent="ThemeOverlay.AppCompat.Dark" />

</resources>
